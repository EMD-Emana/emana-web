import type { MobileNavContent } from './MobileNav.types';

/**
 * Drawer copy. The navigation links themselves are passed in as props by the
 * header, so there is exactly one list of links in the whole site.
 */
export const mobileNavContent: MobileNavContent = {
  openLabel: 'Abrir el menú de navegación',
  closeLabel: 'Cerrar el menú de navegación',
  dialogLabel: 'Menú de navegación',
  navLabel: 'Navegación del sitio',
  panelTitle: 'Menú',
};
